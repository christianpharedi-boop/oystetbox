# Dataset candidates

Store one candidate record per prospective discovery/validation dataset in this directory. Candidate records must use the fields defined in `docs/DATASET_SCREENING_0.2.md` and must not contain OysterBox performance results. A candidate is not eligible for scoring until its decision is recorded as `INCLUDE` in the screening ledger and all required frozen artifact checksums are present.
